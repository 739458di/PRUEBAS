# FyraChat — FASE 1 MAPA (Agente M, 2026-09-10, solo lectura)
(resumen; el detalle con archivo:línea está en el transcript de la sesión 09361876)

A. Pantallas: acceso → lista (#scr-lista) → chat (#scr-chat, botonera, copiloto t0, inputbar). Arranque = 4 requests seriales (acceso_yo → tenant_info → chats → timbre_url) + WebSocket del timbre (el puente emite a TODOS los clientes sin filtro de tenant). Estado global: TEL, CHATS, FOCO, TENANT, SESION, TUTO, ULT_TS. Render: pintarLista = innerHTML total en cada cambio; cargarChat trae TODA la conversación la primera vez (hasta 1,416 msgs) + prueba_reset completa.
B. Puertas de envío (todas terminan en el puente /api/send, /api/send-fotos, /tenant/id/delegar+opener, envíos internos): manual_directo, resolver (idempotente por QID), ejecutarAccion (info/fotos/ubicación/cotizar; cita vía SB), delegar, opener_auto→puente, rescates/programados (avanzan estado ANTES de enviar, sin reintento), citas-vivas.enviarWA (bloquea tel de prueba), casillaEjecutar (checklist + claim), SB upload/cita-extractor (sin tenant salvo msj_confirmacion, sin tel-prueba), puente (auto-opener, citas entrantes, carga de lote, recepción, avisos al owner). SIN validación de pertenencia ni idempotency key: manual_directo, resolver(tenant), ejecutarAccion, delegar t0, envíos del SB, ghost/programados/rescates. Solo resolver y casillaEjecutar tienen candado.
C. Delegación: copilot → seb-panel delegar (tenant, catálogo, modo de entrada; Agente G: modos-acción → ejecutarAccion) → puente /tenant/id/delegar (chats_activos, mensaje 'sistema' que crea la portada de conversaciones, delegaciones, opener) → acciones + timbre → lista.
D. Citas: puertas cita_manual (botón / Calendar), cita-extractor (texto), senalManual (t0); máquina matchDirectoCalendar → ejecutarMatch → planRecordatorios → crearCasillas → programarEnPuente → casillaEjecutar (checklist+claim) ← puente y barredor del cron; procesarEntrante por universo. REUTILIZAR TAL CUAL.
E. Rescates (solo t0, tabla rescates, sin casilla), programados (marcan enviado antes de enviar), ghosting viejo apagado. Todos salen por ghost_scan → puente.
F. BUG último mensaje: la base está bien (0 portadas incorrectas en 60 chats). Causa 1 [V]: onTimbre en copilot actualiza/crea la fila por teléfono ANTES de filtrar por tenant y el puente emite a todos → un mensaje de t0 pisa la portada del mismo teléfono en t2 e inserta filas ajenas. Causa 2 [V]: 26 conversaciones t0 con identidad @lid (12 con mapa a teléfono sin fusionar) → filas "+969…" sin nombre con "tu" mensaje a otra persona. Causa 3 [S]: onclick con nombre inyectado.
G. Cuellos: 4 requests seriales al abrir; chat = conversación completa + 8-9 queries; botón = 2-5 s con timbre awaited 2.5 s; cita = 5-15 s síncrono con alert(); delegar = 3 prompts + 3-8 s; pintarLista total; alert/prompt/confirm por todos lados.
H. Reutilizar: puente delegar/soltar, universo.js, casillas/citas, enviarWA, acceso.js, acciones, demo.js, ejecutarAccion, timbre, resolver, guardarMensajeNuevo.
I. Eliminar/consolidar: filtro tenant en timbre (y salas por tenant en el puente), fusión @lid, una función enviarPaquete con idempotency key, rescates/programados → casillas, muertos (ghosting.js, dual-write chats_activos, POLL…, nuevo_chat, parseConversacion, cargarResets por chat), prompt/alert → UI, pintarLista por diff, tenant_info duplicado.
